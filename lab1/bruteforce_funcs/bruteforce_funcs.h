#ifndef BRUTEFORCE_FUNCS
#define BRUTEFORCE_FUNCS

void initPossiblePassword(char* password, int maxLen);

int checkPasswordArray(char* password, int maxLen);

void incPassword(unsigned char* password, int maxLen);

#endif
